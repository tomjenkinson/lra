<?xml version="1.0" encoding="UTF-8"?>
<!--
   Copyright The Narayana Authors
   SPDX-License-Identifier: Apache-2.0
 -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.jboss.narayana.lra</groupId>
  <artifactId>lra-benchmarks</artifactId>
  <version>2.0.0.Final-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>LRA JMH Benchmarks</name>
  <description>JMH-based performance benchmarks for LRA coordinator stress testing, including Bulkhead functionality</description>
  <url>https://www.narayana.io/</url>

  <licenses>
    <license>
      <name>Apache-2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.html</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <id>narayanaAuthors</id>
      <name>Narayana Authors</name>
      <organization>Commonhaus Foundation</organization>
      <organizationUrl>https://www.commonhaus.org/</organizationUrl>
    </developer>
  </developers>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>17</maven.compiler.release>

    <!-- JMH version -->
    <jmh.version>1.37</jmh.version>

    <!-- LRA and dependency versions -->
    <version.microprofile.lra>2.0.2</version.microprofile.lra>
    <version.microprofile.config-api>3.1.1</version.microprofile.config-api>
    <version.microprofile.fault-tolerance>4.1.2</version.microprofile.fault-tolerance>
    <version.microprofile.rest-client-api>4.0</version.microprofile.rest-client-api>
    <version.jakarta.ws.rs.jakarta-ws-rs-api>4.0.0</version.jakarta.ws.rs.jakarta-ws-rs-api>
    <version.jakarta.json-api>2.1.3</version.jakarta.json-api>
    <version.org.jboss.resteasy>6.2.16.Final</version.org.jboss.resteasy>
    <version.org.jboss.logging.jboss-logging>3.6.3.Final</version.org.jboss.logging.jboss-logging>
    <version.smallrye.config>3.17.2</version.smallrye.config>
    <version.smallrye.fault-tolerance>6.8.0</version.smallrye.fault-tolerance>

    <!-- WildFly versions (aligned with parent project) -->
    <version.wildfly>40.0.1.Final</version.wildfly>
    <version.wildfly-maven-plugin>6.0.0.Final</version.wildfly-maven-plugin>

    <!-- WildFly coordinator configuration -->
    <wildfly.home>${env.JBOSS_HOME}</wildfly.home>
    <wildfly.skip>true</wildfly.skip>
    <lra.coordinator.host>localhost</lra.coordinator.host>
    <lra.coordinator.port>8080</lra.coordinator.port>
    <lra.coordinator.url>http://${lra.coordinator.host}:${lra.coordinator.port}/lra-coordinator</lra.coordinator.url>
    <narayana.noupdate>false</narayana.noupdate>

    <!-- Maven plugin versions -->
    <version.maven.compiler-plugin>3.13.0</version.maven.compiler-plugin>
    <version.maven.dependency-plugin>3.11.0</version.maven.dependency-plugin>
    <version.maven.exec-plugin>3.5.0</version.maven.exec-plugin>
    <version.maven.shade-plugin>3.6.0</version.maven.shade-plugin>

    <!-- Benchmark execution -->
    <uberjar.name>lra-benchmarks</uberjar.name>
  </properties>

  <dependencies>
    <!-- JMH dependencies -->
    <dependency>
      <groupId>org.openjdk.jmh</groupId>
      <artifactId>jmh-core</artifactId>
      <version>${jmh.version}</version>
    </dependency>
    <dependency>
      <groupId>org.openjdk.jmh</groupId>
      <artifactId>jmh-generator-annprocess</artifactId>
      <version>${jmh.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- MicroProfile LRA API -->
    <dependency>
      <groupId>org.eclipse.microprofile.lra</groupId>
      <artifactId>microprofile-lra-api</artifactId>
      <version>${version.microprofile.lra}</version>
    </dependency>

    <!-- MicroProfile Fault Tolerance (for Bulkhead) -->
    <dependency>
      <groupId>org.eclipse.microprofile.fault-tolerance</groupId>
      <artifactId>microprofile-fault-tolerance-api</artifactId>
      <version>${version.microprofile.fault-tolerance}</version>
    </dependency>

    <!-- MicroProfile Config -->
    <dependency>
      <groupId>org.eclipse.microprofile.config</groupId>
      <artifactId>microprofile-config-api</artifactId>
      <version>${version.microprofile.config-api}</version>
    </dependency>

    <!-- SmallRye implementations -->
    <dependency>
      <groupId>io.smallrye.config</groupId>
      <artifactId>smallrye-config</artifactId>
      <version>${version.smallrye.config}</version>
    </dependency>
    <dependency>
      <groupId>io.smallrye</groupId>
      <artifactId>smallrye-fault-tolerance</artifactId>
      <version>${version.smallrye.fault-tolerance}</version>
    </dependency>

    <!-- JAX-RS Client API -->
    <dependency>
      <groupId>jakarta.ws.rs</groupId>
      <artifactId>jakarta.ws.rs-api</artifactId>
      <version>${version.jakarta.ws.rs.jakarta-ws-rs-api}</version>
    </dependency>

    <!-- RESTEasy client implementation -->
    <dependency>
      <groupId>org.jboss.resteasy</groupId>
      <artifactId>resteasy-client</artifactId>
      <version>${version.org.jboss.resteasy}</version>
    </dependency>
    <dependency>
      <groupId>org.jboss.resteasy</groupId>
      <artifactId>resteasy-jackson2-provider</artifactId>
      <version>${version.org.jboss.resteasy}</version>
    </dependency>

    <!-- Jakarta JSON -->
    <dependency>
      <groupId>jakarta.json</groupId>
      <artifactId>jakarta.json-api</artifactId>
      <version>${version.jakarta.json-api}</version>
    </dependency>

    <!-- Logging -->
    <dependency>
      <groupId>org.jboss.logging</groupId>
      <artifactId>jboss-logging</artifactId>
      <version>${version.org.jboss.logging.jboss-logging}</version>
    </dependency>

    <!-- LRA Coordinator WAR for WildFly deployment -->
    <dependency>
      <groupId>org.jboss.narayana.lra</groupId>
      <artifactId>lra-coordinator-war</artifactId>
      <version>${project.version}</version>
      <type>war</type>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${version.maven.compiler-plugin}</version>
        <configuration>
          <release>${maven.compiler.release}</release>
          <annotationProcessorPaths>
            <path>
              <groupId>org.openjdk.jmh</groupId>
              <artifactId>jmh-generator-annprocess</artifactId>
              <version>${jmh.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>

      <!-- Build executable JAR with all dependencies -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${version.maven.shade-plugin}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <finalName>${uberjar.name}</finalName>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>org.openjdk.jmh.Main</mainClass>
                </transformer>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                <transformer implementation="org.apache.maven.plugins.shade.resource.AppendingTransformer">
                  <resource>META-INF/BenchmarkList</resource>
                </transformer>
              </transformers>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!--
        WildFly Maven Plugin for coordinator management.
        Use -Pwildfly profile to enable automatic WildFly startup/shutdown
      -->
      <plugin>
        <groupId>org.wildfly.plugins</groupId>
        <artifactId>wildfly-maven-plugin</artifactId>
        <version>${version.wildfly-maven-plugin}</version>
        <configuration>
          <skip>${wildfly.skip}</skip>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      Download and prepare WildFly with updated LRA coordinator.
      Activate with: mvn clean install -Pdownload
    -->
    <profile>
      <id>download</id>

      <properties>
        <wildfly.download>true</wildfly.download>
        <wildfly.home>${project.basedir}/target/wildfly/wildfly-${version.wildfly}</wildfly.home>
        <wildfly.root>${project.basedir}/target/wildfly</wildfly.root>
      </properties>

      <build>
        <plugins>
          <plugin>
            <artifactId>maven-dependency-plugin</artifactId>
            <version>${version.maven.dependency-plugin}</version>
            <executions>
              <execution>
                <id>unpack-wildfly</id>
                <goals>
                  <goal>unpack</goal>
                </goals>
                <phase>generate-resources</phase>
                <configuration>
                  <markersDirectory>${wildfly.home}/dependency-maven-plugin-markers</markersDirectory>
                  <artifactItems>
                    <artifactItem>
                      <groupId>org.wildfly</groupId>
                      <artifactId>wildfly-dist</artifactId>
                      <version>${version.wildfly}</version>
                      <type>zip</type>
                      <overWrite>false</overWrite>
                      <outputDirectory>${wildfly.root}</outputDirectory>
                    </artifactItem>
                  </artifactItems>
                </configuration>
              </execution>
              <execution>
                <id>update-narayana-lra</id>
                <goals>
                  <goal>copy</goal>
                </goals>
                <phase>process-classes</phase>
                <configuration>
                  <skip>${narayana.noupdate}</skip>
                  <silent>false</silent>
                  <artifactItems>
                    <artifactItem>
                      <groupId>org.jboss.narayana.lra</groupId>
                      <artifactId>lra-client</artifactId>
                      <version>${project.version}</version>
                      <type>jar</type>
                      <overWrite>true</overWrite>
                      <outputDirectory>${wildfly.home}/modules/system/layers/base/org/jboss/narayana/lra/lra-participant/main</outputDirectory>
                      <destFileName>lra-client-${project.version}.jar</destFileName>
                    </artifactItem>

                    <artifactItem>
                      <groupId>org.jboss.narayana.lra</groupId>
                      <artifactId>narayana-lra</artifactId>
                      <version>${project.version}</version>
                      <type>jar</type>
                      <overWrite>true</overWrite>
                      <outputDirectory>${wildfly.home}/modules/system/layers/base/org/jboss/narayana/lra/lra-participant/main</outputDirectory>
                      <destFileName>narayana-lra-${project.version}.jar</destFileName>
                    </artifactItem>

                    <artifactItem>
                      <groupId>org.jboss.narayana.lra</groupId>
                      <artifactId>lra-proxy-api</artifactId>
                      <version>${project.version}</version>
                      <type>jar</type>
                      <overWrite>true</overWrite>
                      <outputDirectory>${wildfly.home}/modules/system/layers/base/org/jboss/narayana/lra/lra-participant/main</outputDirectory>
                      <destFileName>lra-proxy-api-${project.version}.jar</destFileName>
                    </artifactItem>
                  </artifactItems>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <!--
      WildFly profile - starts/stops WildFly with LRA coordinator for benchmarking.
      Use together with download profile: mvn clean package -Pdownload,wildfly
      Or with existing JBOSS_HOME: mvn package -Pwildfly
    -->
    <profile>
      <id>wildfly</id>

      <build>
        <plugins>
          <plugin>
            <groupId>org.wildfly.plugins</groupId>
            <artifactId>wildfly-maven-plugin</artifactId>
            <version>${version.wildfly-maven-plugin}</version>
            <configuration>
              <skip>false</skip>
              <jbossHome>${wildfly.home}</jbossHome>
            </configuration>
            <executions>
              <!-- Start WildFly and deploy LRA coordinator -->
              <execution>
                <id>start-wildfly</id>
                <phase>pre-integration-test</phase>
                <goals>
                  <goal>start</goal>
                </goals>
                <configuration>
                  <jbossHome>${wildfly.home}</jbossHome>
                  <serverConfig>standalone.xml</serverConfig>
                </configuration>
              </execution>

              <!-- Deploy LRA coordinator WAR -->
              <execution>
                <id>deploy-coordinator</id>
                <phase>pre-integration-test</phase>
                <goals>
                  <goal>deploy-artifact</goal>
                </goals>
                <configuration>
                  <groupId>org.jboss.narayana.lra</groupId>
                  <artifactId>lra-coordinator-war</artifactId>
                  <name>lra-coordinator.war</name>
                </configuration>
              </execution>

              <!-- Undeploy and stop WildFly -->
              <execution>
                <id>undeploy-coordinator</id>
                <phase>post-integration-test</phase>
                <goals>
                  <goal>undeploy</goal>
                </goals>
                <configuration>
                  <match-pattern>lra-coordinator.*</match-pattern>
                  <failOnError>false</failOnError>
                </configuration>
              </execution>

              <execution>
                <id>stop-wildfly</id>
                <phase>post-integration-test</phase>
                <goals>
                  <goal>shutdown</goal>
                </goals>
              </execution>
            </executions>
          </plugin>

          <!-- Execute benchmarks during integration-test phase -->
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>exec-maven-plugin</artifactId>
            <version>${version.maven.exec-plugin}</version>
            <executions>
              <execution>
                <id>run-benchmarks</id>
                <phase>integration-test</phase>
                <goals>
                  <goal>exec</goal>
                </goals>
                <configuration>
                  <executable>java</executable>
                  <arguments>
                    <argument>-jar</argument>
                    <argument>${project.build.directory}/${uberjar.name}.jar</argument>
                    <argument>-p</argument>
                    <argument>coordinatorUrl=${lra.coordinator.url}</argument>
                  </arguments>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
